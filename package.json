{
  "name": "mongoo",
  "version": "0.0.1",
  "main": "index.js",
  "license": "Unlicense",
  "scripts": {
    "fmt": "prettier --single-quote --arrow-parens avoid --tab-width 4 --print-width 80 --write \"{,!(node_modules)/**/}*.js\"",
    "lint": "eslint . --fix",
    "test": "ava"
  },
  "dependencies": {
    "mongodb": "^3.0.5",
    "mongoose": "^5.0.11"
  },
  "devDependencies": {
    "ava": "^0.25.0"
  }
}
